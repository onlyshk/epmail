%
% | EPmail configuration file
%

%% Smtp server name
{smtp_server_name, "_"}.

%% Pop3 server name
{pop3_server_name, "_"}.

%% Smtp port
{smtp_port, 25}.

%% POP3 port
{pop3_port, 110}.

%% Smtp server start: start | not_start
{smtp_server_start, start}.

%% POP3 server start: start | not_start
{pop3_server_start, start}.

%% Error logger path
{error_logger_path, logger}.

%% Domain name local storage
{domain, ["localhost"]}.

%% User storage dets || mnesia || ets
{user_storage, dets}.
